{
  "name": "ocr_browser_extension",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "mkdir dist; rm --recursive --force dist/* && cp --recursive extension/* dist && webpack"
  },
  "devDependencies": {
    "@babel/core": "^7.10.4",
    "@babel/preset-env": "^7.10.4",
    "babel-loader": "^8.0.6",
    "webpack": "^4.41.6",
    "webpack-cli": "^3.3.12"
  },
  "dependencies": {
    "lit-html": "^1.2.1"
  }
}
